# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
plan.json
crash.log

# Backend config is machine-generated per learner (bootstrap.sh)
labs/03-foundation/backend.hcl

# Python
__pycache__/
*.pyc
.venv/

# Local scratch
*.zip
.DS_Store
lab1-evidence.json

# Operator config (personal data: owner email) — see config.env.example
config.env
# Local evidence scratch (the committed, redacted proofs live in evidence/)
.scratch/

# Saved plans embed prior state (keys, connection strings) — never commit
*.plan
tf.plan
plan-output.txt
plan.txt
